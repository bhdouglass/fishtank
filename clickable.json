{
  "arch": "all",
  "template": "custom",
  "build": "../click/build.sh",
  "kill": "webapp-container",
  "scripts": {
    "server": "cd www; http-server -c-1",
    "build-android": "CORDOVA=true gulp build && cordova -d build android --device"
  },
  "specificDependencies": true,
  "dependencies": [
    "nodejs",
    "nodejs-legacy"
  ]
}
